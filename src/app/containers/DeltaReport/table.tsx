import * as React from 'react';
import {IBeneficiaryDeltaReport} from 'models/report';

interface IProps {
  report: IBeneficiaryDeltaReport;
}

export const DeltaTable = (p: IProps) => {
  return (
    <div>hi</div>
  );
};
